import { SingleHostElementAttributeDirective } from './single-host-element-attribute.directive';

describe('SingleHostElementAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleHostElementAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
